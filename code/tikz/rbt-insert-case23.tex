\documentclass[tikz]{standalone}
\usetikzlibrary{arrows, positioning}
\tikzset{
  treenode/.style = {align=center, inner sep=1pt, text centered,
    font=\sffamily},
  arn_b/.style = {treenode, circle, white, font=\sffamily\bfseries, draw=black,
    fill=black, text width=2.5em},% arbre rouge noir, noeud noir
  arn_r/.style = {treenode, circle, red, draw=red, 
    text width=2.5em, very thick}% arbre rouge noir, noeud rouge
}
\tikzstyle{every node}=[font=\Large]
\begin{document}
\begin{tikzpicture}[level/.style={sibling distance = 6cm/#1,
    level distance = 1.5cm}]
    \node (lefttree) {}
     child {node [arn_b] {$C$} 
            child {node [arn_r] {$A$}
            child {node {$\alpha$}}
            child {node (anb) [arn_r] {$B$}
                child {node {$\beta$}}
                child {node {$\gamma$}}
            }
        }
        child {node (ansigma) {$\sigma$}}
     }
    ;
    \node [right] at (anb.east) {$z$};
    \node [right] at (ansigma.east) {$y$};
    \node [below, yshift=-6.5cm] at (lefttree) {Case 2};

    \draw [-stealth, line width=0.4mm, draw=blue](2,-1) -- (4,-1)node[midway,above,shape=rectangle,draw=none]{};

    \node (midtree) [right=of lefttree, xshift=5cm] {}
        child {node [arn_b] {$C$}
            child{node [arn_r] {$B$}
                child{node (bna) [arn_r] {$A$}
                    child{node {$\alpha$}}
                    child{node {$\beta$}}
                }
                child{node {$\gamma$}}
            }
            child{node (bnsigma) {$\sigma$}}
        }
    ;
    \node [right] at (bna.east) {$z$};
    \node [right] at (bnsigma.east) {$y$};
    \node [below, yshift=-6.5cm] at (midtree) {Case 3};

    \draw [-stealth, line width=0.4mm, draw=blue](8,-1) -- (10,-1)node[midway,above,shape=rectangle,draw=none]{};

    \node (righttree) [right=of midtree, xshift=4cm] {}
        child{node [arn_b] {$B$}
            child{node (cna) [arn_r] {$A$}
                child{node {$\alpha$}}
                child{node {$\beta$}}
            }
            child{node [arn_r] {$C$}
                child{node {$\gamma$}}
                child{node {$\sigma$}}
            }
        }
    ;
    \node [right] at (cna.east) {$z$}; 
\end{tikzpicture}
\end{document}